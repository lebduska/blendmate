import { PanelProps } from '@/types/panels.ts';

export default function ChatPanel(_props: PanelProps) {
  return (
    <div className="p-4 text-sm text-muted-foreground">Chat panel is not yet implemented.</div>
  );
}
